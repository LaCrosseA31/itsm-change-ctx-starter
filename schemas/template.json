{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "StandardChangeTemplate",
  "description": "A pre-approved template. If an RFC matches a template, it can be auto-approved as a standard change (subject to overriding rules).",
  "version": "1.0.0",
  "owner": "CAB chair + Compliance officer (co-owned)",
  "type": "object",
  "required": ["id", "name", "match_patterns", "allowed_service_tiers"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^TPL-[A-Z0-9-]+$"
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "match_patterns": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Keywords in RFC title that indicate a template match"
    },
    "allowed_service_tiers": {
      "type": "array",
      "items": {"type": "string", "enum": ["critical", "standard", "non-critical"]},
      "description": "Which service tiers this template can apply to"
    },
    "version": {
      "type": "integer"
    }
  }
}
